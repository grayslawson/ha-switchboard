#include <tunables/global>

profile ha_switchboard flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>
  #include <abstractions/python>

  capability net_bind_service,
  network inet stream,
  network inet dgram,
  network inet6 stream,
  network inet6 dgram,

  /usr/local/bin/python3 mr,
  /usr/local/bin/python3.* mr,
  /app/** rix,
  /data/ r,
  /data/** rwk,
  /etc/** r,
  /proc/** r,
  /sys/** r,
  /tmp/** rw,

  deny /root/** rwklx,
  deny /home/** rwklx,
  deny /config/** rwklx,
  deny /ssl/** rwklx,
  deny /share/** rwklx,
  deny /media/** rwklx,
}
